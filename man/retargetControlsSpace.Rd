% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectControls.R
\name{retargetControlsSpace}
\alias{retargetControlsSpace}
\title{Point a prepared control space at a different set of cases.}
\usage{
retargetControlsSpace(space, casesMean, casesPDs)
}
\arguments{
\item{space}{a list as returned by \code{\link{prepareControlsSpace}}.}

\item{casesMean}{numeric vector of case means in the PCA-like space, one
value per reduced component.}

\item{casesPDs}{numeric matrix of case principal directions in the PCA-like
space (rows are reduced components, columns are principal directions).}
}
\value{
\code{space} with its \code{mean} and \code{cov} replaced.
}
\description{
\code{\link{prepareControlsSpace}} splits into a part that depends only on
the controls -- the reduced coordinates, which are the expensive bit -- and a
part that describes the cases, namely the mean and covariance the subsample
should match. Only the latter changes when the same controls are matched
against one case cluster after another, as
\code{\link{selectControlsHier}} does, so the coordinates can be computed
once and re-aimed.
}
\seealso{
\code{\link{prepareControlsSpace}}
}
