% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hier.R
\name{selectControlsHierFromFiles}
\alias{selectControlsHierFromFiles}
\title{Match a case hierarchy without holding the control genotypes in memory.}
\usage{
selectControlsHierFromFiles(
  genotypeFile,
  originalFile,
  cases,
  SVDReference,
  controlsMean,
  clusterMergeCoef = 1.1,
  softMinLambda = 0.9,
  softMaxLambda = 1.05,
  projectionThreads = NULL,
  ...
)
}
\arguments{
\item{genotypeFile}{path of the control matrix file holding the imputed
genotypes.}

\item{originalFile}{path of the control matrix file holding the raw genotypes
(\code{dtype = "u8"}) or the collapsed per-cluster counts.}

\item{cases}{result of calling function readInstanceFromYml.}

\item{SVDReference}{reference basis of the left singular vectors.}

\item{controlsMean}{mean value of the reference genotypes.}

\item{clusterMergeCoef}{numeric coefficient of preference of merging clusters.}

\item{softMinLambda}{desirable minimum for lambda.}

\item{softMaxLambda}{desirable maximum for lambda.}

\item{projectionThreads}{optional integer number of threads for the streaming
projection. By default uses all available hardware threads.}

\item{...}{parameters to be passed to selectControls function.}
}
\description{
Does what \code{\link{selectControlsHier}} does, reading the controls from
files written by \code{\link{writeControlsMatrix}} instead of taking them as
matrices, exactly as \code{\link{selectControlsFromFiles}} does for a single
set of cases. Because the control side is prepared once for the whole
hierarchy, each file is read once no matter how many nodes the hierarchy has.
}
\details{
With a collapsed counts file the selected controls come back as cluster
identifiers, and the merge heuristic weighs each of them by the number of
samples its cluster holds.
}
\seealso{
\code{\link{selectControlsHier}}, \code{\link{writeControlsMatrix}},
\code{\link{collapseControlsMatrix}}
}
